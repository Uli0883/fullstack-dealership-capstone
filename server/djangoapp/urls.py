from django.urls import path
from . import views

urlpatterns = [
    # Aquí van las rutas de la app, por ejemplo:
    # path('', views.index, name='index'),
    # path('dealers/', views.dealers, name='dealers'),
]